# remotes::install_github("thinkr-open/checkhelper")
# checkhelper::print_globals()


globalVariables(unique(c(
  # f_data_input:
  "Direction", "Freq", "iCode", "iName", "Level", "Parent", "Type", "Var1", "Weight",
  # f_plot_framework:
  "outW",
  # f_plot_map2:
  "geometry", "Level", "providers", "uCode",
  # get_solo_groups:
  "x"
)))
